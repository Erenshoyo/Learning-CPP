#include <iostream>
#include <cmath> //For math functions
int main() {
    double x = 3;
    double y = 4;
    double z = 5;

    // z = std::max(x,y); //"max" function returns the maximum of two values.
    // z = std::min(x, y); //"min" function returns the minimum of two values.
    // z = pow(x, y);
    // z = sqrt(y);
    // z = abs(-x); //absolute value of x.
    // z = round(x); //rounds to the nearest integer.
    // z = ceil(3.1); //rounds up to the nearest integer.
    // z = floor(3.99); //rounds down to the nearest integer.
    std::cout << z;
    return 0;
}